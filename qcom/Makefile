
dtb-$(CONFIG_ARCH_KALAMA) += kalama-rumi.dtb \
	     kalama-mtp.dtb \
	     kalama-cdp.dtb \
	     kalama-qrd.dtb

always-y        := $(dtb-y)
subdir-y        := $(dts-dirs)
clean-files     := *.dtb *.dtbo
